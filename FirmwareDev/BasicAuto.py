from .ControlHardware import ControlHardware
       
        """
        Directly moves a specific servo.
        - servo_number: The output pin on the flight controller (e.g., 1, 2, 3...)
        - pwm_value: The position to move to. Usually 1000 (min), 1500 (center), 2000 (max).
        """

control_hardware = ControlHardware()

def translate_channels()


while True:
        driver_commands = control_hardware.get_driver_inputs()



        